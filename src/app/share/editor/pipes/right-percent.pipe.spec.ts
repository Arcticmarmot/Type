import { RightPercentPipe } from './right-percent.pipe';

describe('RightPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new RightPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
